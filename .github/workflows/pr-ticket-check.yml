name: PR Ticket Check

on:
  pull_request:
    branches: [dev, prod]
    types: [opened, edited, synchronize, reopened]

permissions:
  contents: read

jobs:
  ticket-check:
    uses: ./.github/workflows/_reusable-pr-ticket-check.yml
