name: PR Ticket Check

on:
  pull_request:
    branches: [dev]
    types: [opened, edited, synchronize, reopened]

permissions:
  contents: read

jobs:
  ticket-check:
    uses: OrcheSignt/orchsight-devops-cloud-platform/.github/workflows/_reusable-pr-ticket-check.yml@main
